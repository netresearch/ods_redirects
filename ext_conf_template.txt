# cat=basic; type=string; label=Language detection: Used to add L parameter to page ID redirects. Enter a semicolon divided list with search=language pairs, e.g. "/en/=0".
lang_detect =

# cat=basic; type=boolean; label=Use sorting as priority instead of mode and creation order
sorting_is_priority = 0

# cat=basic; type=string; label=Regex delimiter
regex_delimiter = /